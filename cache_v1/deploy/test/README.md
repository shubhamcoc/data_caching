# Steps to run the test application to simulate POST request

1. Install the pre-requisite package using the following command

   ```
   $ pip3 install -r requirements.txt
   ```

2. Start the application using the following command

   ```
   $ python3 server.py
   ```
